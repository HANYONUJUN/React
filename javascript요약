let과 const는 기본적으로 var를 대체한다.
arrow function은 javascript환경에서 함수를 생성하는 또 다른 방법이다.
다른 파일의 기능에 계속 엑세스하려면 export(available하게 하기위해) 및 import 엑세스를 확보하기위해 statements가 필요
classes는 constructor함수와 prototypes를 대체하는 기능
